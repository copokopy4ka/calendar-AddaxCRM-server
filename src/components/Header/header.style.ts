import { styled } from '@stitches/react';

export const HeaderWrapper = styled('header', {
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
  width: '100%',
  marginBottom: '34px',
});

export const CreateTaskAndLabelButtons = styled('div', {
  display: 'flex',
  gap: '10px',
});
