import { styled } from '@stitches/react';

export const UserEventsCalendarWrapper = styled('div', {
  width: '100%',
  height: '100%',
  display: 'flex',
  flexDirection: 'column',
  alignItems: 'center',
});
